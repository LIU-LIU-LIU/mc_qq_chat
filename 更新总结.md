# MC QQ Chat 插件 - 更新完成

## 问题诊断

您遇到的问题主要有以下几个方面：

1. **Velocity 不兼容** - 原插件只支持 BungeeCord，Velocity 使用不同的插件消息协议
2. **API 版本过旧** - Paper 1.20.2 API 无法在 1.21 上正常工作
3. **聊天事件已弃用** - `AsyncPlayerChatEvent` 在新版本中已被 `AsyncChatEvent` 替代
4. **消息格式化问题** - 使用了旧的 BungeeCord Chat API

## 已完成的修改

### 1. 添加 Velocity 支持

#### 新增文件：
- `src/main/java/cc/ahaly/mc/mc_qq_chat/velocity/VelocityMain.java` - Velocity 主类
- `src/main/java/cc/ahaly/mc/mc_qq_chat/velocity/VelocityFun.java` - Velocity 消息处理
- `src/main/resources/velocity-plugin.json` - Velocity 插件配置

#### 特性：
- 使用 Velocity 3.3+ API
- 支持插件消息通道：`mc_qq_chat:bungeecord`
- 使用 Adventure Component 进行消息格式化
- 完整的 QQ 频道集成

### 2. 升级到 Paper 1.21 API

#### 修改文件：
- `pom.xml` - 更新依赖到 Paper 1.21.1
- `src/main/java/cc/ahaly/mc/mc_qq_chat/bukkit/MessageSend.java` - 使用新的聊天事件
- `src/main/java/cc/ahaly/mc/mc_qq_chat/bukkit/MessageReceived.java` - 使用 Adventure API

#### 变更：
- 使用 `AsyncChatEvent` 替代 `AsyncPlayerChatEvent`
- 使用 `net.kyori.adventure.text.Component` 替代 `net.md_5.bungee.api.chat.TextComponent`
- 使用 `GsonComponentSerializer` 进行消息序列化

### 3. 代理适配器模式

#### 新增文件：
- `src/main/java/cc/ahaly/mc/mc_qq_chat/util/qqbot/ProxyAdapter.java` - 代理适配器接口

#### 修改文件：
- `src/main/java/cc/ahaly/mc/mc_qq_chat/util/qqbot/WebSocketApi.java` - 支持适配器
- `src/main/java/cc/ahaly/mc/mc_qq_chat/bungee/BungeeMain.java` - 实现适配器
- `src/main/java/cc/ahaly/mc/mc_qq_chat/velocity/VelocityMain.java` - 实现适配器

#### 好处：
- 解耦 WebSocketApi 与具体代理服务器实现
- 同时支持 BungeeCord 和 Velocity
- 便于未来扩展其他代理服务器

## 如何使用

### 编译插件

```bash
mvn clean package
```

编译后的 jar 文件位于 `target/mc_qq_chat-1.2-SNAPSHOT.jar`

### 部署方式

#### 对于 BungeeCord 用户（保持兼容）

1. 将 jar 文件放入 BungeeCord 的 `plugins` 文件夹
2. 将同一个 jar 文件放入所有 Bukkit/Paper 后端的 `plugins` 文件夹
3. 在 BungeeCord 的 `plugins/mc_qq_chat/config.yml` 配置 QQ 机器人信息
4. 重启所有服务器

#### 对于 Velocity 用户（新支持）

1. 将 jar 文件放入 Velocity 的 `plugins` 文件夹
2. 将同一个 jar 文件放入所有 Bukkit/Paper 后端的 `plugins` 文件夹
3. 在 Velocity 的 `plugins/mc_qq_chat/config.yml` 配置 QQ 机器人信息
4. 重启所有服务器

**重要**：同一个 jar 文件可以同时在 BungeeCord、Velocity 和 Paper 服务器上使用！

## 配置文件

`config.yml` 示例：

```yaml
# 日志级别: info, warning, fine, finest
log-level: info
logFile: "plugins/mc_qq_chat/run.log"

# QQ 机器人令牌，格式：Bot APPID.Token
botToken: "Bot 102.abcdxxxx"

# 频道索引（如果有多个主频道）
guildsIndex: 0

# 子频道名称
channelName: "服务器内部聊天(与游戏内聊天同步)"

# 频道加入链接
channelUrl: "https://pd.qq.com/s/8tjjog2zh"

# 消息事件类型：MESSAGE_CREATE 或 AT_MESSAGE_CREATE
intents: "MESSAGE_CREATE"
```

## 功能特性

### 1. 跨服聊天同步
- ✅ 所有后端服务器之间的聊天自动同步
- ✅ 显示消息来源服务器
- ✅ 支持悬停查看服务器信息
- ✅ 点击服务器名可切换服务器

### 2. QQ 频道集成
- ✅ 游戏聊天自动同步到 QQ 频道
- ✅ QQ 频道消息自动同步到游戏
- ✅ 支持 QQ 机器人命令（`@机器人 /glist`）
- ✅ 显示玩家头像和昵称

### 3. 富文本消息
- ✅ 悬停显示玩家延迟
- ✅ 悬停显示服务器玩家列表
- ✅ 点击玩家名执行命令（如传送请求）
- ✅ 点击查看玩家统计数据
- ✅ 完整的颜色和格式支持

## 技术架构

```
┌─────────────────────┐
│   QQ 频道机器人      │
└──────────┬──────────┘
           │ WebSocket
           │
┌──────────▼──────────┐
│  Velocity/BungeeCord│
│   (代理服务器)       │
│                     │
│  - VelocityMain     │
│  - BungeeMain       │
│  - WebSocketApi     │
│  - ProxyAdapter     │
└──────────┬──────────┘
           │ Plugin Message Channel
           │ (mc_qq_chat:bungeecord)
           │
    ┌──────┴──────┬──────────┬──────────┐
    │             │          │          │
┌───▼───┐   ┌────▼────┐ ┌───▼───┐ ┌───▼───┐
│ Paper │   │  Paper  │ │ Paper │ │ Paper │
│ 1.21  │   │  1.21   │ │ 1.21  │ │ 1.21  │
│       │   │         │ │       │ │       │
│ 生存服 │   │  创造服  │ │ 小游戏 │ │ 大厅   │
└───────┘   └─────────┘ └───────┘ └───────┘
```

## API 版本对比

| 组件 | 旧版本 | 新版本 |
|------|--------|--------|
| Paper API | 1.20.2 | 1.21.1 |
| Velocity API | - | 3.3.0 |
| 聊天事件 | AsyncPlayerChatEvent | AsyncChatEvent |
| 文本组件 | BungeeCord Chat | Adventure API |
| 序列化器 | ComponentSerializer | GsonComponentSerializer |

## 兼容性

- ✅ Minecraft 1.21.x
- ✅ Paper 1.21+
- ✅ Velocity 3.3+
- ✅ BungeeCord 1.20+
- ✅ Java 17+

## 测试建议

### 1. 基础功能测试
- [ ] 在 Paper 服务器上发送聊天消息
- [ ] 验证消息是否同步到其他服务器
- [ ] 验证消息是否发送到 QQ 频道

### 2. QQ 频道测试
- [ ] 在 QQ 频道发送消息
- [ ] 验证消息是否出现在所有游戏服务器
- [ ] 测试 `@机器人 /glist` 命令

### 3. 富文本功能测试
- [ ] 悬停在玩家名上查看延迟
- [ ] 点击服务器名切换服务器
- [ ] 点击玩家名触发命令

### 4. Velocity 特定测试
- [ ] 验证插件在 Velocity 上正常加载
- [ ] 验证插件消息通道正常工作
- [ ] 测试服务器切换功能

## 故障排除

### 问题：插件无法加载
**解决方案**：
- 确认 Java 版本为 17+
- 检查服务器版本是否匹配
- 查看 `plugins/mc_qq_chat/run.log` 日志

### 问题：消息不同步
**解决方案**：
- 确认所有服务器都安装了插件
- 检查插件消息通道是否正确注册
- 设置日志级别为 `fine` 查看详细日志

### 问题：QQ 机器人无法连接
**解决方案**：
- 验证 `botToken` 格式是否正确（`Bot APPID.Token`）
- 检查网络连接和防火墙设置
- 确认 QQ 机器人权限配置正确

## 更新日志

### v1.2-SNAPSHOT (当前版本)
- ✨ 新增 Velocity 代理服务器支持
- ⬆️ 升级到 Paper 1.21 API
- 🔄 使用 Adventure Component API
- 🏗️ 重构代理适配器模式
- 🐛 修复聊天格式化问题
- 📝 完善文档和注释

## 下一步计划

- [ ] 添加更多 QQ 机器人命令
- [ ] 支持私聊回复功能
- [ ] 添加消息过滤和黑名单
- [ ] 支持多频道同步
- [ ] 添加性能监控和统计

## 贡献

欢迎提交 Issue 和 Pull Request！

## 许可证

[根据您的项目添加许可证信息]

